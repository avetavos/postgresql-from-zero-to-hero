---
title: PostgreSQL ขั้นสูง
description: PostgreSQL เป็นมากกว่าตาราง — เอกสาร JSONB, full-text search, view และตรรกะที่เก็บไว้ในฐานข้อมูล เปลี่ยนมันให้กลายเป็นแพลตฟอร์มสำหรับแอปพลิเคชัน ภาพรวมของสิ่งที่โมดูลนี้ครอบคลุม
sidebar:
  order: 1
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

มาถึงตอนนี้คุณสร้างตารางได้ เขียนคำสั่ง CRUD ได้ join แถวที่เกี่ยวข้องกันได้ และเข้าใจเรื่อง index แล้ว นั่นคือแกนหลักเชิงสัมพันธ์ (relational core) และสำหรับหลายแอปพลิเคชันแค่นี้ก็เพียงพอ แต่ PostgreSQL ยังมาพร้อมกับฟีเจอร์อีกชั้นหนึ่งอย่างเงียบ ๆ ที่ทำให้ฐานข้อมูลเดียวสามารถทำงานที่คนมักจะหาบริการเสริมมาจัดการได้ ไม่ว่าจะเป็นการเก็บเอกสารที่ยืดหยุ่น การค้นหาข้อความภาษาธรรมชาติ การแคชผลลัพธ์ของ query ที่มีต้นทุนสูง และการรันตรรกะภายในตัวเซิร์ฟเวอร์เอง

โมดูลนี้คือทัวร์ของฟีเจอร์ชั้นที่สองนั้น ไม่มีอะไรในนี้ที่มาแทนที่สิ่งที่คุณรู้อยู่แล้ว แต่มันวางอยู่บนตาราง คอลัมน์ และ query ธรรมดา เป้าหมายคือให้คุณรู้จักสังเกตว่าเมื่อใดที่ฟีเจอร์ในตัวของ PostgreSQL จะช่วยให้คุณไม่ต้องไปต่อชิ้นส่วนเพิ่มเติมเข้ามา

## Postgres เป็นมากกว่าตาราง

ตารางธรรมดาเก็บคอลัมน์ที่ตายตัวด้วยชนิดข้อมูลที่ตายตัว ฟีเจอร์ในโมดูลนี้ขยายภาพนั้นออกไปสี่ทิศทาง โดยทั้งหมดไม่ต้องออกไปจากฐานข้อมูล

export const platformDiagram = `flowchart TD
  T[(Ordinary tables)] --> J[JSONB columns: flexible documents]
  T --> F[Full-text search: rank natural language]
  T --> V[Views and materialized views: reusable and cached queries]
  T --> L[Functions and triggers: logic inside the server]
  J --> P[One database, many jobs]
  F --> P
  V --> P
  L --> P`;

<Mermaid code={platformDiagram} title="สี่วิธีที่ Postgres ขยายขีดความสามารถของตารางธรรมดา" />

แต่ละกิ่งตอบโจทย์ความต้องการในโลกจริงที่ต่างกัน คอลัมน์ `jsonb` ทำให้แถวเดียวเก็บเอกสารซ้อนกัน (nested document) ที่รูปร่างต่างกันได้ในแต่ละแถว full-text search เปลี่ยนข้อความยาว ๆ ให้กลายเป็นสิ่งที่คุณค้นหาได้ตามความหมายแทนที่จะเป็นการเทียบ substring แบบเป๊ะ ๆ view ตั้งชื่อให้กับ query ที่ซับซ้อน และ materialized view จดจำผลลัพธ์ของมันไว้ ส่วน function และ trigger ย้ายตรรกะชิ้นเล็ก ๆ ให้มาอยู่ติดกับข้อมูล ทำให้มันทำงานอย่างสม่ำเสมอไม่ว่าแอปพลิเคชันใดจะมาแตะตารางนั้น

## เปรียบเทียบกันแบบรวดเร็ว

มันช่วยได้ถ้าเราเห็นในพริบตาว่าแต่ละฟีเจอร์แก้ปัญหาอะไรและมีต้นทุนอะไรบ้าง

| Feature                 | แก้ปัญหา                                  | ข้อแลกเปลี่ยนหลัก                          |
|-------------------------|-----------------------------------------|-----------------------------------------|
| `jsonb` columns         | ข้อมูลที่ยืดหยุ่น ซ้อนกัน และมี schema เบา     | การรับประกันชนิดข้อมูลอ่อนกว่าคอลัมน์จริง     |
| Full-text search        | ค้นหาแบบจัดอันดับบนภาษาธรรมชาติ           | ต้องตั้งค่า language configuration ให้ถูก  |
| Views                   | ตั้งชื่อและนำ query ที่ซับซ้อนมาใช้ซ้ำ           | คำนวณใหม่ทุกครั้งที่อ่าน                  |
| Materialized views      | แคชผลลัพธ์ของ query ที่มีต้นทุนสูง            | ผลลัพธ์ล้าสมัยจนกว่าจะ refresh           |
| Functions and triggers  | ตรรกะฝั่งเซิร์ฟเวอร์ที่รันเสมอ              | พฤติกรรมที่ซ่อนอยู่ซึ่งต้องบันทึกเอกสารไว้ |

## โมดูลนี้ครอบคลุมอะไรบ้าง

- **JSONB** — เก็บเอกสารที่ยืดหยุ่นไว้ในคอลัมน์ เข้าถึงข้างในด้วย operator อย่าง `->` และ `->>` แก้ไขมันด้วย `jsonb_set` และเร่งความเร็ว containment query ด้วย GIN index
- **Full-text search** — เปลี่ยนข้อความให้เป็น `tsvector` จับคู่กับ `tsquery` ด้วย operator `@@` จัดอันดับผลลัพธ์ด้วย `ts_rank` และทำ index เพื่อความเร็ว
- **Views และ materialized views** — ตั้งชื่อให้ query ด้วย `CREATE VIEW` หรือแคชผลลัพธ์ของมันด้วย `CREATE MATERIALIZED VIEW` แล้ว refresh เมื่อต้องการ
- **Functions และ triggers** — เขียน PL/pgSQL function เล็ก ๆ ที่คืนค่า แล้วต่อ trigger เพื่อให้มันรันโดยอัตโนมัติเมื่อมี `INSERT` หรือ `UPDATE`

## Tips / ข้อควรระวัง

- หยิบฟีเจอร์เหล่านี้มาใช้เมื่อมันเหมาะ ไม่ใช่เพราะมันมีอยู่ คอลัมน์ธรรมดาดีกว่าฟิลด์ `jsonb` เสมอเมื่อรูปร่างของข้อมูลรู้แน่และคงที่
- ชั้นนี้ส่วนใหญ่ก็ยังเป็น SQL อยู่ดี คุณจะยังเขียน `SELECT`, `INSERT` และ `WHERE` ต่อไป — ชิ้นส่วนใหม่ ๆ เพียงแค่เสียบเข้าไปในคำสั่งที่คุณเข้าใจอยู่แล้ว
- ทุกฟีเจอร์ขั้นสูงล้วนมีเรื่องราวของ index ในตัว การรู้ว่าเมื่อใดควรเพิ่ม GIN index มักเป็นเส้นแบ่งระหว่างฟีเจอร์ที่ scale ได้กับฟีเจอร์ที่คลานต้วมเตี้ยม
- ตรรกะที่อยู่ในฐานข้อมูลนั้นทรงพลังแต่ลืมง่าย จงบันทึกเอกสารของทุก view, function หรือ trigger เพื่อให้คนต่อไปรู้ว่ามันมีอยู่

<Callout title="แบบจำลองทางความคิด">
ลองนึกภาพ PostgreSQL ว่าเป็นแกนหลักเชิงสัมพันธ์ที่มีของเสริมแบบเลือกได้ซึ่งมากับกล่องอยู่แล้ว คุณไม่ได้กำลังเรียนฐานข้อมูลตัวใหม่ในโมดูลนี้ — คุณกำลังเรียนว่าจะหยิบเครื่องมือในตัวชิ้นไหนมาใช้เพื่อหลีกเลี่ยงการเพิ่ม search engine, document store หรือ cache แยกต่างหากเข้าไปใน stack ของคุณ
</Callout>

export const quizIndex = [
  {
    q: 'ฟีเจอร์ใดของ PostgreSQL เหมาะที่สุดสำหรับเก็บเอกสารที่ยืดหยุ่นและซ้อนกัน ซึ่งรูปร่างต่างกันได้ในแต่ละแถว?',
    options: ['view', 'คอลัมน์ jsonb', 'trigger', 'tsvector'],
    answer: 1,
  },
  {
    q: 'ข้อแลกเปลี่ยนหลักของ materialized view เมื่อเทียบกับ view ธรรมดาคืออะไร?',
    options: [
      'มันทำ index ไม่ได้',
      'ผลลัพธ์ที่เก็บไว้จะล้าสมัยจนกว่าคุณจะ refresh',
      'มันเก็บได้แค่หนึ่งแถว',
      'มันคำนวณใหม่ทุกครั้งที่อ่าน',
    ],
    answer: 1,
  },
  {
    q: 'แก่นร่วมของทุกฟีเจอร์ในโมดูลนี้คืออะไร?',
    options: [
      'แต่ละอย่างต้องใช้บริการภายนอก',
      'พวกมันแทนที่ตารางธรรมดาไปทั้งหมด',
      'พวกมันขยายขีดความสามารถของตารางธรรมดาโดยไม่ต้องออกไปจากฐานข้อมูล',
      'พวกมันทำงานได้แค่ใน psql',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="advanced-postgres/index" questions={quizIndex} />

<ProgressTracker client:visible id="advanced-postgres/index" />
