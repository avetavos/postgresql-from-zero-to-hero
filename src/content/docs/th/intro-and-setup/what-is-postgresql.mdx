---
title: PostgreSQL คืออะไร
description: relational model ของ table และความสัมพันธ์ การรับประกันแบบ ACID, MVCC ในหนึ่งบรรทัด ความสามารถในการขยายที่ทำให้ Postgres แตกต่าง และเมื่อใดที่มันเป็นเครื่องมือที่ใช่
sidebar:
  order: 2
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

PostgreSQL คือ relational database: มันเก็บข้อมูลเป็น **table** และให้คุณอธิบายความสัมพันธ์ระหว่าง table เหล่านั้นได้ บนรากฐานนี้มันยังเพิ่มการรับประกันความถูกต้องที่แข็งแกร่งและความยืดหยุ่นในระดับที่ไม่ธรรมดา ซึ่งเมื่อรวมกันแล้วก็อธิบายได้ว่าทำไมมันจึงเป็นตัวเลือกเริ่มต้นสำหรับโปรเจกต์จำนวนมาก บทเรียนนี้อธิบายแนวคิดหลักเพื่อให้ส่วนที่เหลือของคอร์สมีพื้นฐานที่มั่นคงให้ยืน

## relational model

ใน relational model ข้อมูลอยู่ใน table โดย **table** คือตารางที่มีชื่อ แต่ละ **column** มีชื่อและ type ที่ตายตัว และแต่ละ **row** คือหนึ่ง record ที่มีค่าสำหรับทุก column table ของลูกค้าอาจมี column สำหรับ id, ชื่อ และอีเมล โดยแต่ละ row คือลูกค้าหนึ่งราย

พลังนั้นมาจาก **ความสัมพันธ์ (relationship)** row ใน table หนึ่งสามารถชี้ไปยัง row ใน table อีกอันได้โดยการเก็บ key ของมัน table `orders` เก็บ `customer_id` ของลูกค้าที่สั่งซื้อในแต่ละ order ซึ่งเชื่อมโยง table ทั้งสองเข้าด้วยกัน แผนภาพด้านล่างแสดง table สอง table ที่สัมพันธ์กันในลักษณะนี้

export const relationDiagram = `flowchart LR
  subgraph customers
    C1[id: 1, name: Mira]
    C2[id: 2, name: Devon]
  end
  subgraph orders
    O1[id: 100, customer_id: 1]
    O2[id: 101, customer_id: 1]
    O3[id: 102, customer_id: 2]
  end
  O1 -->|customer_id references id| C1
  O2 -->|customer_id references id| C1
  O3 -->|customer_id references id| C2`;

<Mermaid code={relationDiagram} title="table สองอันที่สัมพันธ์กันด้วย key" />

id ที่ระบุ row ได้แบบไม่ซ้ำคือ **primary key** ของมัน column ใน table อีกอันที่ชี้กลับมาที่มันคือ **foreign key** คุณจะออกแบบ table และ key ในโมดูลถัดไป และรวมมันด้วย join ในภายหลัง สำหรับตอนนี้สิ่งที่ต้องจำคือความสัมพันธ์ถูกเก็บเป็นค่าธรรมดา ไม่ใช่ pointer ที่ซ่อนอยู่

## การรับประกันแบบ ACID

PostgreSQL รับประกันว่าข้อมูลของคุณยังคงถูกต้องผ่านการล่ม ข้อผิดพลาด และการเข้าถึงพร้อมกัน คำสัญญาเหล่านั้นสรุปได้ด้วยคำย่อ **ACID**

| ตัวอักษร | คุณสมบัติ | มันหมายถึงอะไรสำหรับคุณ |
|--------|----------|------------------------|
| A | Atomicity | กลุ่มของการเปลี่ยนแปลงจะเกิดขึ้นทั้งหมดหรือไม่เกิดเลย |
| C | Consistency | ทุกการเปลี่ยนแปลงทำให้ฐานข้อมูลยังคงปฏิบัติตามกฎของมัน |
| I | Isolation | transaction ที่เกิดพร้อมกันจะไม่ทำให้กันและกันเสียหาย |
| D | Durability | เมื่อ commit แล้ว การเปลี่ยนแปลงจะอยู่รอดผ่านการล่ม |

หน่วยที่นำพาการรับประกันเหล่านี้คือ **transaction** — กลุ่มของ statement ที่ถูกปฏิบัติเสมือนเป็นการทำงานแบบทั้งหมดหรือไม่ทำเลยหนึ่งเดียว transaction จะมีโมดูลเต็มในภายหลัง สำหรับตอนนี้ให้รู้ไว้ว่า PostgreSQL จะไม่ทิ้งข้อมูลของคุณไว้ในสภาพที่เปลี่ยนไปเพียงครึ่งเดียว

## MVCC ในหนึ่งบรรทัด

PostgreSQL บรรลุ isolation ผ่าน **MVCC** หรือ multi-version concurrency control: แทนที่จะ lock row สำหรับผู้อ่าน มันเก็บ row หลายเวอร์ชันเพื่อให้ผู้อ่านเห็น snapshot ที่สอดคล้องกันในขณะที่ผู้เขียนทำงาน และผู้อ่านไม่เคยขวางผู้เขียน

## ความสามารถในการขยาย (Extensibility)

สิ่งที่ทำให้ PostgreSQL แตกต่างอย่างแท้จริงคือคุณขยายมันได้มากแค่ไหน คุณไม่ได้ถูกจำกัดอยู่กับเมนูฟีเจอร์ที่ตายตัว

- **type ที่หลากหลายและกำหนดเองได้** — นอกเหนือจากตัวเลขและข้อความ Postgres มาพร้อมกับ date, array, geometric type, network address และอื่น ๆ และคุณสามารถนิยาม type ของคุณเองได้
- **JSONB** — เก็บและสร้าง index ให้เอกสาร JSON จริงภายใน column ได้ ผสมผสานสไตล์ relational และ document ไว้ในฐานข้อมูลเดียว
- **Extension** — แพ็กเกจแบบเสียบใช้งานได้ทันทีเพิ่มความสามารถทั้งชุด เช่น `postgis` สำหรับข้อมูลทางภูมิศาสตร์ หรือ `pg_trgm` สำหรับการจับคู่ข้อความแบบ fuzzy ติดตั้งได้ด้วยคำสั่ง `CREATE EXTENSION` เพียงคำสั่งเดียว

ความยืดหยุ่นนี้หมายความว่า PostgreSQL instance เดียวมักจะแทนที่เครื่องมือเฉพาะทางหลายตัวได้ ทำให้ stack ของคุณเรียบง่ายขึ้น

## เมื่อใดที่ PostgreSQL เหมาะสม

PostgreSQL เป็นตัวเลือกเริ่มต้นที่แข็งแกร่งสำหรับเกือบทุกแอปพลิเคชันที่เก็บข้อมูลแบบมีโครงสร้างและใส่ใจเรื่องความถูกต้อง: backend ของเว็บและมือถือ, analytics, เครื่องมือภายใน และอื่น ๆ มันโดดเด่นเมื่อข้อมูลของคุณมีความสัมพันธ์ที่ชัดเจน เมื่อคุณต้องการ transaction ที่ไว้ใจได้ หรือเมื่อคุณต้องการ engine เดียวที่จัดการ JSON, search และงาน geospatial ได้ด้วย

มันเหมาะน้อยลงเมื่อคุณต้องการ in-memory cache ล้วน ๆ สำหรับข้อมูลชั่วคราว หรือ engine เฉพาะทางสำหรับ workload สุดขั้วเพียงอย่างเดียว เช่น time-series ที่อัตราการรับเข้าข้อมูลมหาศาล — แม้แต่ในกรณีนั้น extension มักจะช่วยปิดช่องว่างได้ สำหรับโปรเจกต์ส่วนใหญ่อย่างท่วมท้น การหยิบ PostgreSQL ขึ้นมาก่อนเป็นตัวเลือกที่ปลอดภัยและยืนยง

## เคล็ดลับ / จุดที่ต้องระวัง

- คิดในรูปของ table และความสัมพันธ์ตั้งแต่แรก การออกแบบข้อมูลให้ดีตั้งแต่เนิ่น ๆ ช่วยลดความเจ็บปวดในภายหลัง
- primary key ระบุ row ได้แบบไม่ซ้ำ ส่วน foreign key ใน table อีกอันชี้กลับมาที่มัน
- ACID ไม่ใช่เวทมนตร์ที่คุณเปิดสวิตช์ — มันเปิดอยู่เสมอ นำพาโดย transaction
- หยิบ JSONB มาใช้เมื่อส่วนหนึ่งของข้อมูลของคุณเป็น schemaless อย่างแท้จริง แต่ให้เก็บส่วนที่มีโครงสร้างไว้ใน column ปกติ
- ก่อนที่จะรับเอาฐานข้อมูลเฉพาะทางแยกต่างหากมาใช้ ให้ตรวจดูว่ามี extension ของ PostgreSQL ที่ครอบคลุมความต้องการนั้นอยู่แล้วหรือไม่

<Callout title="relational ก่อน ยืดหยุ่นเสมอ">
relational model มอบความถูกต้องและโครงสร้างที่ชัดเจนให้คุณ ACID มอบความไว้วางใจ ส่วน extensibility มอบพื้นที่ให้เติบโต PostgreSQL เป็นเครื่องมือที่หาได้ยากที่มอบทั้งสามอย่างพร้อมกัน ซึ่งเป็นเหตุผลว่าทำไมส่วนใหญ่ของคอร์สนี้จึงเป็นเพียงการเรียนรู้ที่จะใช้ฐานข้อมูลหนึ่งให้ดีจริง ๆ
</Callout>

export const quizWhatIs = [
  {
    q: 'ใน relational model อะไรเป็นที่เก็บข้อมูล?',
    options: ['Document', 'Table ที่ประกอบด้วย row และ column', 'คู่ key-value', 'Graph ของ node'],
    answer: 1,
  },
  {
    q: '"D" ใน ACID รับประกันอะไร?',
    options: [
      'ข้อมูลถูกลบหลังจากแต่ละ session',
      'เมื่อ commit แล้ว การเปลี่ยนแปลงจะอยู่รอดผ่านการล่ม',
      'ทศนิยมถูกเก็บอย่างแม่นยำ',
      'row ที่ซ้ำกันจะถูกปฏิเสธ',
    ],
    answer: 1,
  },
  {
    q: 'ฟีเจอร์ใดให้คุณเก็บและสร้าง index ให้เอกสาร JSON จริงภายใน column ได้?',
    options: ['MVCC', 'JSONB', 'Foreign key', 'Atomicity'],
    answer: 1,
  },
];

<Quiz client:visible id="intro-and-setup/what-is-postgresql" questions={quizWhatIs} />

<ProgressTracker client:visible id="intro-and-setup/what-is-postgresql" />
